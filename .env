# 浏览器端变量必须使用 VITE_ 前缀

# 项目标题
VITE_APP_TITLE = Vue Admin Template

# 路由模式：hash 或 html5
VITE_ROUTER_HISTORY = hash

# 是否启用本地 Mock API
VITE_USE_MOCK = true

# Node 脚本变量不使用 VITE_ 前缀
# Swagger / OpenAPI JSON 地址；也可通过 api:generate -- --url=... 临时指定
SWAGGER_URL =
